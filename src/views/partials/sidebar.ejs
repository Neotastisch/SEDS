<!-- Sidebar -->
<div class="fixed inset-y-0 left-0 w-64 bg-white shadow-lg">
    <div class="flex items-center justify-center h-16 border-b">
        <h1 class="text-xl font-bold text-gray-800">SEDS</h1>
    </div>
    <nav class="mt-4">
        <a href="/dashboard" class="flex items-center px-6 py-3 text-gray-700 <%= page === 'dashboard' ? 'bg-gray-100' : 'hover:bg-gray-50' %>">
            <i class="fas fa-th-large mr-3"></i>
            Dashboard
        </a>
        <a href="/monitoring" class="flex items-center px-6 py-3 text-gray-700 <%= page === 'monitoring' ? 'bg-gray-100' : 'hover:bg-gray-50' %>">
            <i class="fas fa-chart-line mr-3"></i>
            Monitoring
        </a>
        <a href="/settings" class="flex items-center px-6 py-3 text-gray-700 <%= page === 'settings' ? 'bg-gray-100' : 'hover:bg-gray-50' %>">
            <i class="fas fa-cog mr-3"></i>
            Settings
        </a>
    </nav>
</div> 